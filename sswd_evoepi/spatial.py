"""Spatial and connectivity module — stub.

Handles: node definitions, metapopulation network assembly, C matrix
(larval connectivity), D matrix (pathogen dispersal), SST/salinity
forcing (monthly → daily interpolation), waterway distances.

Build target: Phase 5.
"""
